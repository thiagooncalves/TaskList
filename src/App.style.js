import { styled } from "../src/stitches.config.js";

export const Divmain = styled("div", {
    display: "flex",
  });
  
export const Div = styled("div", {
    display: "flex",
    flexDirection: "column",
    marginLeft: 100,
    fontFamily: "Exo",
  });